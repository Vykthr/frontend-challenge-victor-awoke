import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'frontend-challenge-victor-awoke',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
